import { EquipementPipe } from './equipement.pipe';

describe('EquipementPipe', () => {
  it('create an instance', () => {
    const pipe = new EquipementPipe();
    expect(pipe).toBeTruthy();
  });
});
