import { FrencheuroPipe } from './frencheuro.pipe';

describe('FrencheuroPipe', () => {
  it('create an instance', () => {
    const pipe = new FrencheuroPipe();
    expect(pipe).toBeTruthy();
  });
});
