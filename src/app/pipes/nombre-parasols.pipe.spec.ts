import { NombreParasolsPipe } from './nombre-parasols.pipe';

describe('NombreParasolsPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreParasolsPipe();
    expect(pipe).toBeTruthy();
  });
});
